name: publish
on:
  release:
    types: [published]
jobs:
  publish-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        uses: Ilshidur/action-discord@0.3.2
        with:
          args: >
            🎉 Fleeting Notes {{ github.event.release.tag_name }} Release Announcement 🎉

            I'm excited to announce the release of Fleeting Notes {{ github.event.release.tag_name }}! This update includes the following features and improvements:

            ${{ github.event.release.body }}

            You can download the latest version of Obsidian at https://www.fleetingnotes.app/download. I hope you enjoy these new additions and please let me know if you have any feedback!

            Thank you for your continued support!

            Matthew Wong
